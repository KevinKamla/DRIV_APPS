import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.webdevor',
  appName: 'Driv',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
